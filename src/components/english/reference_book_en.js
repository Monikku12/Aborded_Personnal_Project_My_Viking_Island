import * as React from "react";
import { View, Text } from "react-native";

function ReferenceBookEn() {
    return (
        <View
            style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
        >
            <Text>ReferenceBook Screen</Text>
        </View>
    );
}

export default ReferenceBookEn;