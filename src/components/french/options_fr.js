import * as React from "react";
import { View, Text } from "react-native";

function OptionsFr() {
    return (
        <View
            style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
        >
            <Text>Options Screen</Text>
        </View>
    );
}

export default OptionsFr;